{
  "name": "shaker-backup-server",
  "version": "9.0.0",
  "description": "SHAKER local backup server",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "SHAKER_TOKEN=dev_secret node server.js",
    "dev:win": "set SHAKER_TOKEN=dev_secret && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": { "node": ">=16.0.0" }
}
